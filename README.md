# Socket Chat

Java Swing UDP broadcast chat styled after the provided dark UDP console reference.

## Broadcast Address

The app detects available IPv4 broadcast addresses at startup and preselects the first one.

## Run

```powershell
mvn exec:java
```

Default UDP port: `9000`

Type a message and press `Enter`, or use `Send`, to print it in the console.
